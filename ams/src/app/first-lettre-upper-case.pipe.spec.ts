import { FirstLettreUpperCasePipe } from './first-lettre-upper-case.pipe';

describe('FirstLettreUpperCasePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstLettreUpperCasePipe();
    expect(pipe).toBeTruthy();
  });
});
